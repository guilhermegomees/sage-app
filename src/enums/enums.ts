// Tipos de telas
export enum TypeScreem {
    Account = 1,
    Card = 2,
    Transaction = 3,
    Graphics = 4
}

// Tipos de gráficos
export enum charts {
    pie = 1,
    bar = 2,
}

// Contextos de transação
export enum transactionContext {
    revenue = 1,
    expense = 2,
    cardExpense = 3
}